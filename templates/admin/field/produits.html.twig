{% block _Commande_produits %}



{% for produit in field.value %}
<img src="{{ asset('img/produits/' ~ produit.photo) }}" alt="" class="img-fluid" style="max-width:30px;float:left;">
    {{ produit.titre }} / qté : {{ field.value.owner.quantite[produit.id] }}<hr>

    {# {% if role == 'ROLE_ADMIN' %}
        Admin
    {% else %}
        Membre
    {% endif %} #}

{% endfor %}

{% endblock %}